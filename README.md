A sample command-line application providing basic argument parsing with an entrypoint in `bin/`.

# Some ideas

- Generate PDF with workbook
- Generate assignment messages for each part
- interactive cli version to build the yaml file
