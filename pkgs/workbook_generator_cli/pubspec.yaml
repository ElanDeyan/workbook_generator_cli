name: workbook_generator_cli
description: A sample command-line application with basic argument parsing.
version: 0.0.1
publish_to: none
repository: https://github.com/ElanDeyan/workbook_generator_cli

resolution: workspace

environment:
  sdk: ^3.12.0

executables:
  jwb: bin/workbook_generator

# Add regular dependencies here.
dependencies:
  args: ^2.7.0
  dev_utils:
    git:
      url: https://github.com/ElanDeyan/dev_utils
  intl: ^0.20.2
  workbook_generator_core: ^1.0.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  very_good_analysis: ^10.2.0
